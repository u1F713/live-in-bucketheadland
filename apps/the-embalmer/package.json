{
  "name": "@live-in-bucketheadland/the-embalmer",
  "version": "0.0.0",
  "scripts": {
    "build": "rm -rf ./dist && tsc -p ./tsconfig.build.json",
    "dev": "nodemon",
    "test": "jest",
    "test:e2e": "jest --testRegex '.*\\.e2e-spec\\.ts$'",
    "lint": "eslint \"{src,test}/**/*.ts\" --fix"
  },
  "dependencies": {
    "fastify": "^4.5.3"
  },
  "devDependencies": {
    "@live-in-bucketheadland/eslint-config": "workspace:*",
    "@live-in-bucketheadland/tsconfig": "workspace:*",
    "@types/jest": "^28.0.0",
    "@types/node": "^18.7.14",
    "jest": "^28.0.0",
    "nodemon": "^2.0.19",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  }
}
