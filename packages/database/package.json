{
  "name": "@live-in-bucketheadland/database",
  "version": "0.0.0",
  "main": "dist/main.ts",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "tsc",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^4.3.1",
    "prisma": "^4.3.1"
  },
  "devDependencies": {
    "@live-in-bucketheadland/tsconfig": "workspace:*",
    "@types/node": "^18.7.14",
    "typescript": "^4.8.2"
  }
}
